/**
 * Optional catch-all auth routes type as `/sign-in/${slug}`, so the index
 * href that satisfies Next.js typed routes is the trailing-slash form.
 */
export const signInHref = "/sign-in/";
export const signUpHref = "/sign-up/";
